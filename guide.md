# Auto-Deploy 系统使用指南

> **文档版本**: v1.0  
> **适用对象**: 系统使用者（开发人员、运维人员、项目管理者）  
> **最后更新**: 2026-08-15

---

## 目录

- [1. 系统简介](#1-系统简介)
- [2. 快速入门](#2-快速入门)
- [3. 账号与登录](#3-账号与登录)
- [4. 系统导航总览](#4-系统导航总览)
- [5. 部署环境管理](#5-部署环境管理)
- [6. 服务器管理](#6-服务器管理)
- [7. 项目配置管理](#7-项目配置管理)
- [8. 构建与部署](#8-构建与部署)
- [9. 构建历史](#9-构建历史)
- [10. 语言运行时管理](#10-语言运行时管理)
- [11. 系统设置](#11-系统设置)
- [12. 常见问题与排错](#12-常见问题与排错)

---

## 1. 系统简介

Auto-Deploy 是一套自动构建与部署系统，帮助您将代码从 Git 仓库自动编译、打包并部署到远程服务器。

**系统能做什么：**

1. **拉取代码** — 从 Git 仓库（GitHub / GitLab / Gitee 等）自动拉取最新代码
2. **编译构建** — 支持 Java、Go、Node.js、Python 四种语言的自动编译
3. **产物部署** — 将编译结果自动上传到指定的远程服务器并执行重启
4. **实时日志** — 构建过程中实时查看日志输出，就像在终端前看着一样
5. **多环境支持** — 同一项目可以部署到开发、测试、生产等不同环境的服务器
6. **构建队列** — 多个构建任务自动排队，按优先级依次执行

**系统的适用场景：**

- 个人项目或中小团队的持续部署
- 需要频繁构建和部署的项目
- 需要将构建流程标准化、自动化的团队

---

## 2. 快速入门

如果您想尽快完成第一次构建部署，请按以下步骤操作：

### 第一步：准备前置条件

在开始之前，请确认：

- [ ] 系统已启动，可以通过浏览器访问（默认地址 `http://服务器IP:8081`）
- [ ] 已注册账号并登录
- [ ] 已设置好 Git 仓库访问凭证（环境变量）
- [ ] 目标部署服务器的 SSH 密码已设置为环境变量

### 第二步：创建部署环境

1. 点击左侧菜单的 **「部署环境」**
2. 点击 **「新建环境」** 按钮
3. 输入环境名称，例如：`生产环境`、`测试环境`
4. 点击 **「保存」**

### 第三步：添加服务器

1. 点击左侧菜单的 **「服务器维护」**
2. 点击 **「新建服务器」** 按钮
3. 填写服务器信息：
   - **服务器名称**：例如 `生产服务器1号`
   - **服务器类型**：选择 `部署服务器`
   - **所属环境**：选择刚创建的环境
   - **服务器地址**：例如 `192.168.1.100`
   - **SSH 端口**：默认 `22`
   - **用户名**：例如 `root`
   - **认证环境变量名**：填写存储 SSH 密码的环境变量名，例如 `SSH_PASS_PROD`
4. 点击 **「保存」**
5. （可选）点击 **「测试」** 按钮验证连接是否正常

### 第四步：创建项目配置

1. 点击左侧菜单的 **「项目配置」**
2. 点击 **「新建配置」** 按钮
3. 填写项目基本信息：
   - **项目名称**：例如 `我的网站`
   - **版本号**：例如 `1.0`
   - **Git 仓库地址**：例如 `https://github.com/user/repo.git`
   - **代码分支**：例如 `main`
   - **语言类型**：选择 `Java`、`Node` 等
   - **语言版本**：选择已安装的版本
   - **构建指令**：例如 `mvn clean package -DskipTests`
4. 填写部署配置：
   - **项目安装目录**：例如 `/opt/myapp`
   - **部署目标路径**：例如 `releases`（相对于安装目录）
   - **重启命令**：例如 `systemctl restart myapp`
5. 在页面下方的 **「项目环境与服务器配置」** 区域，添加环境和服务器的关联
6. 点击 **「保存」**

### 第五步：发起构建

1. 点击左侧菜单的 **「构建管理」**
2. 在 **「发起构建」** 区域：
   - **选择项目**：选择刚创建的项目
   - **选择分支**：选择要构建的代码分支
   - **选择环境**：勾选目标部署环境
3. 点击 **「开始构建」**
4. 构建任务会出现在下方的任务列表中
5. 点击 **「日志」** 按钮查看实时构建日志

---

## 3. 账号与登录

### 3.1 注册账号

1. 在登录页面点击 **「注册新账号」** 链接
2. 填写以下信息：
   - **用户名**：您的登录用户名
   - **密码**：设置登录密码
   - **确认密码**：再次输入密码确认一致
3. 点击 **「注册」** 按钮
4. 注册成功后自动跳转到登录页面

### 3.2 登录系统

1. 打开浏览器访问系统地址（默认 `http://服务器IP:8081`）
2. 输入用户名和密码
3. 点击 **「登录」** 按钮
4. 登录成功后自动跳转到构建管理页面

> **安全说明**：您的密码在浏览器端就已经加密（SHA-256 哈希），传输到服务器的不是明文密码，请放心使用。

### 3.3 退出登录

点击页面右上角的 **「退出登录」** 按钮即可退出系统。

---

## 4. 系统导航总览

登录后，页面左侧是导航菜单，包含以下功能模块：

| 菜单 | 功能说明 | 使用频率 |
|------|---------|---------|
| **构建管理** | 发起构建任务，查看实时日志和任务状态 | 日常使用 |
| **构建历史** | 查看过去的构建记录，支持筛选查询 | 日常使用 |
| **项目配置** | 创建和管理项目的构建与部署配置 | 初始化/变更时 |
| **服务器维护** | 管理构建服务器和部署服务器 | 初始化/变更时 |
| **部署环境** | 管理部署环境（开发、测试、生产等） | 初始化/变更时 |
| **语言环境** | 查看和安装 Java、Go、Node.js、Python 运行时版本 | 偶尔使用 |
| **系统设置** | 配置系统全局参数（并发数、保留策略等） | 初始化时 |

**建议的初始化顺序**：部署环境 → 服务器维护 → 项目配置 → 系统设置 → 开始构建

---

## 5. 部署环境管理

> 菜单路径：**部署环境**

部署环境用于区分不同的部署目标，例如开发环境、测试环境、生产环境。一个项目可以关联多个环境，每个环境可以包含多台服务器。

### 5.1 创建环境

1. 点击 **「新建环境」** 按钮
2. 在弹出的对话框中输入环境名称，例如：
   - `开发环境`
   - `测试环境`
   - `预发布环境`
   - `生产环境`
3. 点击 **「保存」**

> 环境名称不能重复。

### 5.2 编辑环境

1. 在环境列表中找到要修改的环境
2. 点击 **「编辑」** 按钮
3. 修改环境名称
4. 点击 **「保存」**

### 5.3 删除环境

1. 点击环境对应的 **「删除」** 按钮
2. 确认删除操作

> 删除环境后，与该环境关联的服务器关联关系也会被取消。请谨慎操作。

---

## 6. 服务器管理

> 菜单路径：**服务器维护**

服务器分为两种类型：

- **构建服务器**：用于远程执行构建任务（目前为预留功能）
- **部署服务器**：构建完成后，将产物上传到的目标服务器

### 6.1 添加服务器

1. 点击 **「新建服务器」** 按钮
2. 填写服务器信息：

| 字段 | 说明 | 是否必填 |
|------|------|---------|
| 服务器名称 | 便于识别的名称，如 `生产服务器1号` | 必填 |
| 服务器类型 | `构建服务器` 或 `部署服务器` | 必填 |
| 所属环境 | 部署服务器需要选择所属环境 | 部署服务器必填 |
| 服务器地址 | IP 地址或域名，如 `192.168.1.100` | 必填 |
| SSH 端口 | SSH 服务端口，默认 `22` | 选填 |
| 用户名 | SSH 登录用户名，如 `root` | 选填 |
| 认证环境变量名 | 存放 SSH 密码的**环境变量名称** | 选填 |

3. 点击 **「保存」**

> **关于认证环境变量名**：系统不会直接存储密码。您需要提前在服务器的操作系统中设置环境变量，例如 `export SSH_PASS_PROD=myPassword123`，然后在系统中填写变量名 `SSH_PASS_PROD`。系统在部署时会自动读取该变量的值。

### 6.2 测试服务器连接

添加服务器后，可以点击 **「测试」** 按钮验证 SSH 连接是否正常：

- 连接成功：显示绿色提示 `连接成功！`
- 连接失败：显示红色错误信息，请检查地址、端口、用户名和密码是否正确

### 6.3 编辑和删除服务器

- **编辑**：点击 **「编辑」** 按钮修改服务器信息
- **删除**：点击 **「删除」** 按钮删除服务器（会弹出确认对话框）

---

## 7. 项目配置管理

> 菜单路径：**项目配置**

项目配置是系统的核心，定义了如何从 Git 拉取代码、如何构建、如何部署。

### 7.1 新建项目配置

1. 点击 **「新建配置」** 按钮
2. 填写配置表单（详见下方各字段说明）
3. 点击 **「保存」**

### 7.2 配置字段详细说明

#### 基本信息

| 字段 | 必填 | 说明 |
|------|:---:|------|
| **项目名称** | 是 | 项目的显示名称，如 `用户中心服务` |
| **版本号** | 是 | 当前版本号，如 `2.1.0` |
| **项目标识** | — | 系统自动生成，创建后不可修改。用于内部标识，无需关注 |

#### Git 仓库配置

| 字段 | 必填 | 说明 |
|------|:---:|------|
| **Git 仓库地址** | 是 | 代码仓库的完整地址。支持 HTTPS 和 SSH 两种格式。示例：`https://github.com/user/repo.git` 或 `git@github.com:user/repo.git` |
| **代码分支** | 否 | 要构建的分支名称，默认 `main`。在构建页面也可以临时切换分支 |
| **Git 认证环境变量名** | 否 | HTTPS 仓库填写 Token 的环境变量名（如 `GIT_TOKEN`）；SSH 仓库填写私钥路径的环境变量名。公开仓库无需填写 |

#### 构建配置

| 字段 | 必填 | 说明 |
|------|:---:|------|
| **语言类型** | 是 | 项目使用的编程语言，可选 `Java`、`Go`、`Node`、`Python` |
| **语言版本** | 是 | 构建时使用的语言版本。下拉列表会自动加载系统中已安装的版本。如果列表为空，需要先到「语言环境」页面安装对应版本 |
| **构建指令** | 是 | 编译构建的命令。常见示例：Java: `mvn clean package -DskipTests`；Node: `npm run build`；Go: `go build -o app`；Python: `pip install -r requirements.txt` |
| **构建工作目录** | 否 | 构建指令执行的目录，相对于项目根目录。留空则在项目根目录执行。例如前端项目填 `frontend` |
| **构建产物路径** | 否 | 编译产物的路径模式（支持正则表达式）。通常无需填写，系统会根据语言类型自动检测：Java → `target/` 目录下的 JAR 文件；Node → `dist/`、`build/`、`.next/` 等目录；Go → 可执行文件 |
| **自定义安装目录** | 否 | 语言运行时的自定义安装路径。在 Windows 上选择语言版本时会自动填充对应的安装目录。构建时系统将该目录下的 bin 子目录优先加入 PATH，实现版本切换。一般无需手动填写 |

#### 部署配置

| 字段 | 必填 | 说明 |
|------|:---:|------|
| **项目安装目录** | 否 | 远程服务器上的基础安装目录，必须是绝对路径。例如 `/opt/myapp` |
| **部署目标路径** | 否 | 相对于安装目录的部署路径。例如 `releases`，最终部署路径为 `/opt/myapp/releases` |
| **执行脚本目录** | 否 | 相对于安装目录的脚本目录，如 `scripts` |
| **启动命令** | 否 | 首次部署后执行的启动命令 |
| **重启命令** | 否 | 更新部署后执行的重启命令。例如 `systemctl restart myapp` 或 `pm2 restart myapp` |

#### 环境-服务器关联

在配置页面下方的 **「项目环境与服务器配置」** 区域，可以为项目指定部署目标：

1. 从 **「选择环境」** 下拉列表中选择一个环境
2. 从 **「选择服务器」** 下拉列表中选择该环境下的一台服务器
3. 点击 **「添加」** 按钮
4. 重复以上步骤添加更多环境-服务器组合
5. 已添加的关联会显示在下方表格中，可以点击删除图标移除

> **部署开关**：在项目配置列表页面，每个环境-服务器关联都有一个启用/禁用开关。禁用后该服务器不会参与部署，但关联关系仍然保留。

### 7.3 模块扫描

对于 Java（Maven）和 Go 项目，系统支持自动扫描子模块：

1. 保存项目配置后，页面下方会出现 **「模块扫描」** 区域
2. 点击 **「扫描模块」** 按钮
3. 系统会自动分析项目结构，找出所有子模块
4. 扫描完成后，模块树会显示在页面上
5. 构建时可以选择只部署特定模块

> Node.js 项目不支持模块扫描。

### 7.4 复制配置

如果您有多个相似的项目，可以基于已有配置创建副本：

1. 在项目配置列表中，找到要复制的项目
2. 点击 **「复制」** 按钮
3. 系统会创建一份完全相同的配置副本
4. 修改副本中的项目名称、仓库地址等信息后保存

### 7.5 配置指南

如果您不确定某个字段的含义，可以点击项目配置列表页面的 **「配置指南」** 按钮，查看详细的字段说明文档。

### 7.6 删除配置

1. 在项目配置列表中，点击要删除项目的 **「删除」** 按钮
2. 确认删除操作

> 删除配置不会影响已有的构建历史记录。

---

## 8. 构建与部署

> 菜单路径：**构建管理**

这是系统最核心的功能页面，分为两个区域：上方的 **「发起构建」** 表单和下方的 **「构建任务」** 列表。

### 8.1 发起构建

#### 第一步：选择项目

从 **「选择项目」** 下拉列表中选择要构建的项目。选择后：
- 代码分支列表会自动加载该项目的远程分支
- 如果该项目有子模块，模块选择区域会出现
- 部署环境选择区域会出现

#### 第二步：选择代码分支

从 **「代码分支」** 下拉列表中选择要构建的分支。默认分支会标注 `（当前）`。

> **分支不一致警告**：如果本地仓库有未提交的修改，且目标分支与当前分支不同，系统会阻止构建并提示您先处理本地修改。您可以勾选「跳过代码更新，直接构建」来使用本地当前代码构建。

#### 第三步：选择构建模式

| 模式 | 说明 |
|------|------|
| **本机构建** | 在 Auto-Deploy 所在的服务器上直接执行构建 |
| **远程构建服务器** | 将构建任务发送到远程构建服务器执行（需要先在系统设置中配置构建服务器） |

#### 第四步：选择部署模块（非 Node.js 项目）

如果项目已扫描过模块，会显示模块复选框列表：
- 默认全选所有模块
- 可以取消勾选不需要构建/部署的模块
- 点击 **「全选」** 或 **「清空」** 快速操作

#### 第五步：选择部署环境

勾选本次要部署的目标环境。默认选中第一个环境。

> 只会部署在所选环境中已关联且已启用的服务器。

#### 第六步：开始构建

点击 **「开始构建」** 按钮。系统会先检查是否有冲突：

- **无冲突**：立即开始构建
- **有服务器重叠的任务正在执行**：系统会提示任务将排队等候，自动提交到构建队列
- **已达到并发上限**：任务进入队列等待

### 8.2 查看构建任务

构建任务列表显示所有当前和近期的构建任务，**每 5 秒自动刷新**。

#### 任务状态说明

| 状态 | 颜色 | 含义 |
|------|:---:|------|
| **执行中** | 蓝色 | 构建正在进行中 |
| **排队中** | 黄色 | 任务在队列中等待执行 |
| **成功** | 绿色 | 构建（和部署）已成功完成 |
| **失败** | 红色 | 构建或部署失败 |
| **已取消** | 灰色 | 用户手动取消了任务 |

#### 任务操作

| 操作 | 说明 |
|------|------|
| **日志** | 跳转到构建日志页面，查看实时或历史日志 |
| **停止** | 终止正在执行的构建任务（会弹出确认对话框） |
| **取消** | 取消排队中的任务（会弹出确认对话框） |
| **历史** | 跳转到构建历史页面查看该任务的详细记录 |

#### 搜索和排序

- 点击列标题上的搜索图标可以展开搜索栏，支持按项目名称、用户、状态等筛选
- 点击「提交时间」「完成时间」「优先级」列标题可以排序（升序 → 降序 → 取消排序）

### 8.3 查看构建日志

点击任务列表中的 **「日志」** 按钮进入日志页面。

#### 活跃构建的日志

- 日志通过 SSE（Server-Sent Events）实时推送，可以看到最新的构建输出
- 页面自动滚动到最新日志
- 构建完成后自动加载完整日志

#### 已完成构建的日志

- 显示最近的 500 行日志
- 点击 **「加载更多」** 按钮加载更早的日志内容
- 每次加载 500 行

### 8.4 构建队列

当多个构建任务同时提交时，系统会自动管理构建队列：

- **优先级排序**：优先级高的任务先执行
- **冲突检测**：如果两个任务部署到同一台服务器，后提交的任务会排队等待
- **自动调度**：系统每 5 秒检查一次队列，有空闲时自动执行排队中的任务
- **队列任务**：在任务列表中以「排队中」状态显示，可以随时取消

### 8.5 构建缓存

系统支持构建缓存，如果代码没有变化且构建配置没有修改，系统会跳过构建过程直接使用缓存的产物：

- 缓存判断依据：Git 提交哈希、模块路径、构建命令、语言版本等
- 如果所有条件都匹配，构建会快速完成
- 修改了项目配置后，对应的缓存会自动失效

---

## 9. 构建历史

> 菜单路径：**构建历史**

构建历史页面展示所有过去的构建记录，支持筛选和分页。

### 9.1 查询构建记录

在页面顶部的查询表单中，可以按以下条件筛选：

| 筛选条件 | 说明 |
|---------|------|
| **项目名称** | 输入项目名称的关键词 |
| **状态** | 筛选 `成功` 或 `失败` 的记录 |
| **开始日期** | 选择日期范围 |
| **结束日期** | 选择日期范围 |

点击 **「查询」** 按钮执行筛选。

### 9.2 查看记录详情

每条记录都有两个操作按钮：

- **详情**：弹出详情对话框，显示该次构建的完整信息（Git 哈希、构建命令、产物路径、部署状态等）
- **日志**：弹出日志对话框，查看该次构建的完整日志。支持「加载更多」加载更早的日志

### 9.3 跳转到构建任务

如果某条历史记录关联了构建队列任务，记录中的队列任务 ID 会显示为链接，点击可以跳转到构建管理页面并高亮显示对应的任务行。

### 9.4 从构建管理跳转

在构建管理页面点击任务行的 **「历史」** 按钮，会跳转到构建历史页面并高亮显示对应的记录。

---

## 10. 语言运行时管理

> 菜单路径：**语言环境**

此页面管理系统中安装的编程语言运行时版本。系统支持四种语言：

| 语言 | 版本管理工具 | 说明 |
|------|:----------:|------|
| **Java** | SDKMAN (Unix) / 本地扫描 (Windows) | 管理 JDK 版本 |
| **Go** | gvm (Unix) / 本地扫描 (Windows) | 管理 Go 版本 |
| **Node.js** | nvm | 管理 Node.js 版本 |
| **Python** | uv | 管理 Python 版本 |

### 10.1 查看已安装版本

页面加载时会自动检测每种语言已安装的版本，以绿色标签的形式显示在对应卡片中。

如果某种语言没有安装任何版本，卡片边框会变为黄色并提示 `未安装任何版本`。

**Windows 上的 Java/Go 检测：**

Windows 系统上，Java 和 Go 的版本检测不依赖 SDKMAN 或 gvm，而是通过自动扫描本地安装目录实现。系统会扫描以下位置：

- `JAVA_HOME` / `GOROOT` 环境变量指向的目录
- `C:\Program Files\Java\`、`Eclipse Adoptium\`、`Zulu\`、`Microsoft\` 等常见安装路径
- `%USERPROFILE%\.jdks\`（IntelliJ IDEA 下载的 JDK）

系统启动时会自动扫描一次并缓存结果。

### 10.2 重新扫描

如果您在系统启动后新安装了 Java 或 Go 版本，可以点击页面顶部的 **「重新扫描」** 按钮手动触发扫描：

1. 点击 **「重新扫描」** 按钮
2. 等待扫描完成（通常几秒钟）
3. 页面会自动刷新版本列表
4. 扫描结果会显示检测到的版本数量

### 10.3 检查特定版本

1. 在对应语言卡片的输入框中输入版本号，例如 `17.0.2`（Java）或 `18.17.0`（Node）
2. 点击 **「检查」** 按钮
3. 系统会显示该版本是否已安装

### 10.4 安装新版本

1. 输入要安装的版本号
2. 点击 **「检查」** 确认未安装
3. 点击出现的 **「安装」** 按钮
4. 页面下方的 **「安装日志」** 区域会实时显示安装过程的输出
5. 安装完成后，新版本会自动出现在已安装版本列表中

> **前提条件**：对应的版本管理工具（SDKMAN / gvm / nvm / uv）需要预先在服务器上安装好。

---

## 11. 系统设置

> 菜单路径：**系统设置**

系统设置页面管理全局配置参数。

### 11.1 构建并发配置

设置系统最多可以同时执行多少个构建任务。

- 范围：1 ~ 20
- 默认值：5
- 修改后点击 **「保存」** 生效

> 并发数设置过大会占用过多系统资源，建议根据服务器配置合理设置。

### 11.2 构建任务调度

控制构建队列的自动调度行为：

| 设置 | 说明 |
|------|------|
| **调度状态** | 显示调度器当前是否运行中 |
| **开始调度 / 停止调度** | 手动控制调度器的启停 |
| **常驻模式** | 开启后调度器永远不会自动停止。关闭时，调度器在连续 3 次没有排队任务后会自动停止以节省资源 |

### 11.3 保留策略

| 设置 | 说明 | 默认值 |
|------|------|-------|
| **日志保留天数** | 构建日志文件的保留天数，超过后自动清理 | 7 天 |
| **历史保留天数** | 构建历史记录的保留天数，超过后自动清理 | 90 天 |

> 系统每天凌晨 3 点自动执行清理任务。

### 11.4 构建服务器配置

配置远程构建服务器的连接信息（用于「远程构建」模式）：

| 字段 | 说明 |
|------|------|
| **服务器地址** | 构建服务器的 IP 或域名 |
| **端口** | SSH 端口 |
| **用户名** | SSH 用户名 |
| **认证环境变量名** | SSH 密码对应的环境变量名 |

配置完成后点击 **「测试连接」** 验证是否可用。

### 11.5 环境变量管理

此区域用于注册系统中使用的环境变量名称，方便追踪哪些变量已配置、哪些缺失。

#### 查看环境变量状态

表格列出所有已注册的环境变量：

| 列 | 说明 |
|----|------|
| **变量名** | 环境变量的名称 |
| **描述** | 该变量的用途说明 |
| **状态** | `已设置`（绿色）表示操作系统中已配置该变量；`未设置`（黄色）表示尚未配置 |

#### 添加环境变量引用

1. 点击 **「添加」** 按钮
2. 在弹出对话框中：
   - **环境变量名**：输入变量名（输入框支持自动补全，会列出系统中已有的环境变量）
   - **描述**：可选，填写该变量的用途说明
3. 点击 **「保存」**

#### 删除环境变量引用

点击变量对应的删除图标即可移除注册。

> 删除注册只是从系统的追踪列表中移除，不会影响操作系统中实际的环境变量。

---

## 12. 常见问题与排错

### Q1: 构建失败，提示 Git 拉取失败

**可能原因：**
- Git 仓库地址填写错误
- 认证环境变量未设置或变量名填写错误
- 网络无法访问 Git 仓库

**解决办法：**
1. 检查项目配置中的 Git 仓库地址是否正确
2. 确认 `Git 认证环境变量名` 对应的环境变量已在系统中设置
3. 在系统设置的环境变量管理中确认状态为「已设置」
4. 确认服务器能够访问 Git 仓库（检查网络和防火墙）

### Q2: 构建成功但部署失败

**可能原因：**
- SSH 连接信息不正确
- 目标服务器上没有安装目录的写入权限
- 重启命令执行失败

**解决办法：**
1. 在服务器管理页面使用 **「测试」** 按钮验证 SSH 连接
2. 确认认证环境变量已正确设置
3. 确认项目配置中的安装目录路径正确且有写入权限
4. 查看构建日志中的具体错误信息

### Q3: 语言版本下拉列表为空

**原因：** 对应的语言运行时尚未安装。

**解决办法：**
1. 进入 **「语言环境」** 页面
2. 在对应语言的卡片中输入要安装的版本号
3. 点击 **「检查」** → **「安装」**
4. 等待安装完成后回到项目配置页面，版本列表会自动刷新

### Q4: 构建任务一直显示「排队中」

**可能原因：**
- 调度器未启动
- 有同项目的任务正在执行且存在服务器冲突
- 达到并发上限

**解决办法：**
1. 检查系统设置中的调度状态是否为「运行中」，如未运行则点击 **「开始调度」**
2. 等待当前执行中的任务完成
3. 如需立即执行，可以取消正在排队的任务后重新发起

### Q5: 构建日志页面无法显示实时日志

**可能原因：**
- 构建任务已结束，内存中的任务数据已被清理
- 浏览器不支持 SSE

**解决办法：**
- 如果构建已完成，日志页面会自动切换到文件读取模式，通过「加载更多」按钮查看历史日志
- 确认使用的是现代浏览器（Chrome、Firefox、Edge 等）

### Q6: 如何设置 Git HTTPS 认证？

1. 在服务器操作系统中设置环境变量：`export GIT_TOKEN=your_github_token`
2. 在系统的「系统设置 → 环境变量管理」中添加 `GIT_TOKEN` 并确认状态为「已设置」
3. 在项目配置的 `Git 认证环境变量名` 字段填写 `GIT_TOKEN`

### Q7: 如何设置 SSH 密码认证？

1. 在服务器操作系统中设置环境变量：`export SSH_PASS_PROD=your_password`
2. 在添加服务器时，`认证环境变量名` 字段填写 `SSH_PASS_PROD`
3. 系统在部署时会自动读取该变量的值作为 SSH 密码

### Q8: 系统重启后，正在构建的任务丢失了

这是当前的已知限制。构建任务的运行状态仅保存在内存中，系统重启后无法恢复。重启前建议等待当前任务完成。

### Q9: 如何同时部署到多台服务器？

1. 在项目配置页面，添加多个环境-服务器关联
2. 构建时选择对应的环境，系统会自动部署到该环境下所有已启用的服务器

### Q10: 部署脚本导出功能在哪里？

在构建日志页面，如果构建成功完成，可以下载自动生成的部署脚本（`.bat` 或 `.sh`），用于在脱离系统的情况下手动执行部署。

### Q11: Windows 上如何检测多个 Java 版本？

在 Windows 系统上，Java 版本检测不依赖 SDKMAN，而是通过自动扫描本地安装目录实现：

- 系统启动时会自动扫描一次常见安装路径（`C:\Program Files\Java\`、`JAVA_HOME`、`.jdks\` 等）
- 扫描结果会缓存到内存中，后续访问直接使用缓存
- 如果启动后新安装了 Java 版本，可以在「语言环境」页面点击 **「重新扫描」** 按钮刷新

选择语言版本时，系统会自动填充对应的安装目录，构建时通过设置 PATH 实现版本切换。

### Q12: Windows 上如何切换 Java 版本？

在项目配置页面：

1. 选择语言类型为 `Java`
2. 从版本下拉列表中选择一个已检测到的版本（下拉列表会显示版本号和安装路径）
3. 系统会自动填充「自定义安装目录」字段
4. 保存配置后，构建时会使用该版本的 Java

您也可以手动修改「自定义安装目录」字段指向其他 JDK 安装路径。

---

*文档结束*
